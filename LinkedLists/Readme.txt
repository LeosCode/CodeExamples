These folders contain basic linked list excercises. They use a common include file LinkedList.h that contains common declarations, debugging, and support functions. This allows the exercises to focus on the problem at hand. 

The compiled .exe code is Windows 10 cmd.exe compatable so the examples can be downloaded and run. 

The output result from the .exe is in the Output.txt in the directory.

