These folders contain basic recursion excercises.  

The compiled .exe code is Windows 10 cmd.exe compatable so the examples can be downloaded and run. 

The output result from the .exe is in the Output.txt and OutputDebug.txt in the directory.

